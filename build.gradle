ext.mpsVersion = "2024.1"
ext.languageName = "AlfiSampleLanguage"

ext.mpsPlugins = [  ]

ext.customMpsPlugins = [
    "alfi": "https://github.com/vimotest/mps-alfi/releases/download/1.0.3/alfi.zip",
    "CppBaseLanguage": "https://github.com/Fumapps/CppBaseLanguage/releases/download/2024.1-beta1/CppBaseLanguage.zip"
]

apply from: 'gradle/init-gradle-scripts.gradle'
apply from: 'gradle/alfi/run-alfi-samples.gradle'
apply from: 'gradle/alfi/package-generated-code.gradle'

task prepareMps {
    group 'mps'
    dependsOn 'buildOsSpecificBundle'
}

task fixCopyAlfiRuntimeJarPaths {
    group 'other'
    description 'Workaround for wrong lookup of idea_rt.jar'
    doLast {
        delete "$buildDir/mps-bundle/dependencies/tests"
        copy {
            from "$mpsHome/lib/idea_rt.jar"
            into "$buildDir/mps-bundle/dependencies/tests/"
        }
    }
}

task mpsBuild {
    group 'mps'
    dependsOn 'mpsAntGenerate', 'mpsAntBuild', fixCopyAlfiRuntimeJarPaths
}

task mpsClean {
    group 'mps'
    dependsOn 'mpsAntClean', 'mpsAntCleanSources'
}

task mpsTest {
    group 'mps'
    dependsOn 'mpsAntCheck', 'parseTestReportAfterAntCheck'
}

task openProjectInMps {
    group 'mps'
    dependsOn 'mpsOpenProjectInMpsForCurrentOs'
}

task buildAndOpenProjectInMps {
    group 'mps'
    dependsOn mpsBuild, openProjectInMps
}
